## Create first-java-project using spring.io
## commands to use
```````
## mvn clean install
## mvn archetype:generate -DgroupId=com.rama.app -DartifactId=first-java-project -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false
```````
